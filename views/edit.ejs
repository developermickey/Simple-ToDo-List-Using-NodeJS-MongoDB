<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen px-10 text-white bg-zinc-800 flex flex-col items-center">
        <h3 class="text-center py-10 text-3xl">Update Todo</h3>
        <form class="w-1/2" action="/edit/<%= todo._id %>" method="post">
            <input class="w-full block h-[40px] mt-2 px-3 text-black" type="text" name="newname"
                value="<%= todo.name %>" />
            <textarea class="w-full block h-[60px] mt-5 px-3 text-black"
                name="newcontent"><%= todo.content %></textarea>

            <input class="w-full py-2 block mt-3 px-3 text-black bg-blue-500" type="submit" value="Update" />
        </form>


    </div>
</body>

</html>